package second;

/*import java.util.Scanner;

public class Main {
    public static void main(String[] args) {

        int attempts = (int) (Math.random() * 10);

        //если у пользователя не будет попыток, то это фича

        Main.game(attempts);
    }


    private static void game(int attempts) {
        Scanner scanner = new Scanner(System.in);
        for (int i = 0; i < attempts; i++) {
            if (attempts == 0){
                System.out.println("Lox");
                return;
            }
            else {
                int randomne = (int) (Math.random() * 10);
                int userInput = scanner.nextInt();
                if (userInput < randomne)
                    System.out.println("<");
                else if (userInput > randomne)
                    System.out.println(">");
                else {
                    System.out.println("Won");
                    break;
                }
            }
        }
        System.out.println("U had " + attempts + " attempts");
    }
}
}
*/